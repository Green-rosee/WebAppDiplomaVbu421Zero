namespace WebAppEstimate.Areas.SiteAdam.DTOs.Authz;

public class UserService
{
    /*private readonly List<UserAuthz> _users = new List<UserAuthz>
    {
        new UserAuthz { Login = "admin", Password = "admin", Role = "admin" },
        new UserAuthz { Login = "userAdam1", Password = "userAdam1", Role = "user" },
    };*/

    /*public UserAuthz Authenticate(string login, string password)
    {
        return _users.FirstOrDefault(u => u.Login == login && u.Password == password);
    }*/
    
}